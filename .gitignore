# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Generated analysis reports — written into the project's reports/ subfolder on
# every run and fully reproducible, so the whole subfolder is untracked (inventory
# files like letters.csv and docs like SOURCE.md live outside it and stay).
projects/**/reports/

# Other
.DS_Store

# Internal design/planning notes — local working documents, not user-facing docs
docs/future_directions.md
docs/reconstruction_sketch.md
docs/rule_induction_plan.md
docs/rule_induction_sketch.md
